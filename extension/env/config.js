// ============================================
// EXTENSION CONFIGURATION
// ============================================
// Edit this file to point the extension at your
// own API deployment.
// ============================================

const ENV = {
  // Base URL of the Tesco Price Tracker API.
  // The extension appends /products/{tpnc} to this.
  // Trailing slash is NOT required.
  API_BASE_URL: "https://price-tracker.gavaller.com/api/tesco",
};

export default ENV;
