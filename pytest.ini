[pytest]
testpaths = tests recomendation-system/tests
addopts = --import-mode=importlib
# Several tests assert on the INFO and DEBUG records their code emits. Without
# this, caplog only sees a record when something else has already lowered the
# root logger's level, so those assertions depend on what ran before them.
log_level = DEBUG
